Looks like it tries to write out a loose object when it reads something from a packfile. This is fixed in the development version, but just mount the source repository read-write for now as a work-around.

Fixes #622.
